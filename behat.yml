default:
    suites:
        default:
            contexts:
                - FeatureContext
                - BackgroundContext
                - CucumberContext
        background:
            contexts:
                - BackgroundContext
        cucumber:
            contexts:
                - CucumberContext
        pending:
            contexts:
                - PendingContext
